import React from "react";
import styled from "styled-components";

const Logo: React.FC = styled.p`
  position: absolute;
  top: 40px;
  left: 50%;
  transform: translateX(-50%);
  font-weight: 700;
  font-size: 64px;
  line-height: 78px;
  color: #000000;
`;

export default Logo;
